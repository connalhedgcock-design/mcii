---
id: log.20260826i
t: log
v: 1
---
# 2026-08-26 LIVE MONITORING (operator: "needs to be live, extremely time sensitive")

## MEASURED FIRST, THEN BUILT
- dexscreener: 30 rapid calls, 30 ok / 0 fail, 3.0s = **10 calls/sec sustained**. jupiter 10/10.
- ∴ live on-chain data is free and entirely feasible. no reason to look at a stale price.

## !! THE SPLIT THAT MATTERS
- LIVE (free): price, liquidity, volume, buy/sell flow, safety status, exit capacity. all on-chain.
- NOT LIVE (metered): social. X is the ONLY paid source. 40min IS what $12/mo buys. real-time X ≈ $350/mo. impossible.
- ✓ fortunate split: the genuinely urgent things (liq draining, dev dumping, rug in progress) are the FREE ones.
  social sentiment arriving 3min sooner changes no decision. see G-01.

## ! FRAMING HELD, NOT ABANDONED
- live monitoring is worth a lot for NOTICING DANGER FAST, ~nothing for BUYING FIRST.
- entry speed = millisecond race vs colocated bots. making our app faster does not enter that race, and shouldn't. see D-20.
- ∴ live is pointed at the EXIT. stated plainly to operator rather than quietly selling "fast" as edge.

## TIERS (main/live.js)
- 15s: market data, 1 call/token
- change-triggered: exit sim re-runs only when liquidity moves >=5% ∵ 17 calls/token every 15s would burn quota to learn nothing
- 10min: safety re-check (structural facts change rarely)
- 5min: history WRITE throttle — ! polling fast + writing fast would add ~5,760 rows/token/day and destroy bounded storage. caught before shipping.
- instant alerts: liquidity -15% in one tick = CRITICAL 'this is what a rug looks like while it happens'; price -12% = HIGH; live safety-flip = CRITICAL.
  15min cooloff on live alerts (vs 6/24h on history alerts) ∵ these describe now.

## UI
- numbers patched IN PLACE, never re-rendered ∵ a full redraw every 15s collapses open sections and fights the user mid-scroll.
- tick flash on change, pulse dot per card, alerts slide in at top. all respect prefers-reduced-motion.

## SMOKE TEST 60s: 12 updates, exit sim fired once then cached, safety once then cached, 1 history row/token. correct on every tier.

## STILL NOT RUNNING
- ! screener is STILL not scheduled and still not stored. built + run manually once. D-34 blocks shipping it as an opportunity list until discovery stops being momentum-based.
