---
id: log.20260826c
t: log
v: 1
---
# 2026-08-26 ALERTS BUILT
7 rules, all fire off RECORDED HISTORY not single readings:
- exit-drop HIGH: sellable amount -20%+ ! the one that matters most
- liq-drain HIGH: pool liquidity -25%+  ∵ liquidity leaves before price does
- position-exceeds-exit HIGH: held value > exitable value. fires immediately, needs no history.
- safety-flip CRITICAL: PASS > not-PASS between readings
- holders-falling MED -5%+ | concentrating MED top10 +15%+ | volume-collapse MED <35% of prior
- native OS notifications for CRITICAL/HIGH only, w/ cooloff (6h crit / 24h high) ∴ standing conditions don't re-nag into being ignored.
- ! silence is explicitly disambiguated in UI: "no alerts" vs "needs 2+ readings". a quiet engine and a broken engine must not look alike.

## VERIFICATION
- ! live run fired 0 alerts (only 3 readings, minutes apart, nothing moved) — CORRECT but unfalsifiable on its own.
- ∴ wrote test/alerts.test.js: synthetic inputs for every rule + a NEGATIVE CONTROL (healthy token must stay silent). 7/7 pass. `npm test`.
- ! lesson worth keeping: a rule engine returning zero because nothing happened is indistinguishable from one returning zero because it's broken. always prove it fires.

## J7 TRACKER — evaluated, integration REJECTED
- j7tracker.io. login-gated, creds via discord. NO public API, no docs, no export ∴ integration technically impossible w/o ToS-breaking session scraping.
- pitch: "sub-10ms server-side deploys... can't afford to be a second late" = latency-competitive sniping. DIFFERENT GAME.
  ! that game is against colocated bots. unwinnable by a human clicking a mouse. told operators plainly.
- ! structural: J7 has its own pump.fun token. a tool with a token has an interest in you trading. noted neutrally.
- ✓ IDEA WORTH STEALING, inverted: they track deployers to buy FAST. we should track deployers to check whether a creator has launched before and what happened to those tokens.
  jupiter already returns the `dev` address on every token (have it for CATE). prior-launch history is FREE on-chain data.
  > queued as next build after alerts. arguably higher rug-prevention value than alerts were.

## D-21 REVERSED SAME DAY — deployer check NOT built
- operator: "we can use that with j7 tracker its fine". accepted.
- ! MY PROPOSAL WAS WEAKER THAN I PITCHED IT. I called it "free on-chain data" but wallet>all-tokens-created needs an INDEXED api (helius etc, paid). free tiers may not expose it. I had not verified before recommending. operator's call is better founded than my suggestion was.
- tradeoff stated once, then dropped: J7 is MANUAL. gate runs itself + can alert; a manual check gets skipped precisely when excitement is highest.
- revisit trigger logged in D-21. not raising again. see [[mandate]] DECISION HYGIENE.
